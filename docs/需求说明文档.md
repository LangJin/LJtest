# 测谈网需求说明文档
## 版本变更

版本号 | 作者 | 状态| 变更内容  
-|-|-|-
 V1.03| 浪晋 | 新建 |修改了一些约定|
 V1.04| 浪晋 | 修改 |细化了后台功能的描述|


## 目录
[TOC]

## 第一章 概述
本文档是的需求说明文档，主要的作用是为系统的研发和测试起到说明和指导作用。
测谈网是一个综合性的用于软件测试学习交流的一个网站，主要面向的对象是对软件测试有兴趣的用户，为用户提供一个专业的软件测试的学习门户。
测谈网的主要功能包括了用户提问（交流讨论）、用户发表文章（心得体会）、用户发表灵感（灵光一闪）、用户学习教程等主要功能。
## 第二章 客户端功能说明
### 游客操作
没有登录的用户全部都是游客、游客只具备浏览的权限，并不能有其他的操作。
如果游客想要查看学习教程则跳转到登录页面。
### 用户操作
#### 注册
用户注册必须要填写用户名，用户密码，再次确认密码，手机号，邮箱。
用户名不能重复并且不能少于5位字符多于12位字符，用户名不能输入符号，仅数字和字母组成。
密码长度8-16位，仅限“0123456789qazwsxedcrfvtgbyhnujmikolpQAZWSXEDCRFVTGBYHNUJMIKOLP_!@#$%^&*<>?-=+|”。
手机号，邮箱不能重复绑定。
用户名和用户密码即为登录名和登录密码。
注册成功直接跳转到登录成功的页面，不需要重新登录。
#### 登录
用户输入正确的账号、密码后正常登录。
用户输入错误的密码连续三次，用户账号被锁定，且不可登录。
账号密码不能为空。
#### 设置密保
用户登录后，在个人主页可以通过密保功能设置密保。
如果用户未设置密保，每次用户进入个人主页都将提示用户设置密保。
设置密保需要选择三个不同的问题，并且分别输入答案。
用户也可以自定义问题。
用户自定义的问题仅用户自己可见。
问题描述不能大于20个字。
答案不得大于8个字。
#### 找回密码
用户忘记密码或者账号被锁定可以找回密码，通过验证密保修改密码。
用户输入需要重置密码的账号，输入重置的密码，
答对三个问题即可重置密码成功。
用户修改密码后，就可以用新密码重新登录了，并且取消锁定状态。
#### 修改个人资料
用户登录成功后，可以在个人中心修改自己的个人资料，包括了昵称、电话、邮箱、职业、年龄、性别、个人签名。
用户可以在个人中心修改自己的密码，修改密码需要校验当前密码通过。
用户可以在个人中心修改自己的头像。
用户可以在个人中心修改自己的背景图。
密码规则和注册保持一致。
昵称、职业不能大于8个字，不能为空。
电话、邮箱不能重复。
性别仅男、女、保密。
年龄只是能数字、且不能大于120。
个人签名不能超过20个字。

#### 提问
用户登录成功后，可以进行提问操作，点击提问按钮，跳转到新建问题页面。
用户需要输入问题题目，问题介绍，问题标签、封面图、内容。
用户提问完成后，可以对自己提交的问题进行修改。
题目不能为空、不能超过50个字，其他可以不限。
封面图非必填，不上传时将显示默认图片。
介绍不能超过120字。
标签只能选择已存在标签，用户可以单独设置新的标签。
用户设置的新标签仅自己可见。
标签要求不能大于6个字，不能少于4个字。
内容不限。

#### 发表文章
用户登录成功后，可以写文章。
点击写文章按钮可以跳转到新建文章页面。
用户需要输入文章题目，文章内容，文章标签、文章介绍、封面图。
发表成功后，用户可以对自己写的文章进行删除、修改。
题目不能为空、不能超过50个字，其他可以不限。
封面图非必填，不上传时将显示默认图片。
介绍不能超过120字。
标签只能选择已存在标签，用户可以单独设置新的标签。
用户设置的新标签仅自己可见。
标签要求不能大于6个字，不能少于4个字。
内容不限。
#### 发表灵感
用户登录成功后，可以发表灵感。
点击写灵感按钮跳转到灵感新建页面。
发布内容必填，图片非必填。
上传图片不能超过3张。
内容不能超过200字。
#### 评论
用户登录成功后，可以对任何文章、问题、灵感、教程进行评论。
评论内容不能超过500个字，不能为空。
#### 关注
用户可以关注其他用户、文章、教程、问题。
也可以取消关注。
#### 收藏
用户可以收藏文章、教程、问题、灵感。
也可以取消收藏。
#### 点赞
用户可以对文章、教程、灵感、问题进行点赞。
也可以取消点赞。
#### 用户主页
用户在自己的个人主页可以查看自己的粉丝列表和关注列表。
可以查看自己发表的文章、问题、灵感。
主页会显示用户的操作动态，包括了关注、点赞、收藏、发表内容。



## 第三章 管理员端功能说明
### 首页
首页显示系统的一些数据的统计，暂时未开发
### 轮播图管理
轮播图模块是对用户首页的轮播图进行管理。
进入轮播图列表可以查看所有未删除的轮播图的列表。
管理员可以对轮播图进行修改，可以修改轮播图的标题、外链、图片。
可以对轮播图的状态进行设置、禁用或者启用。
可以对轮播图进行删除，删除后不显示在列表中。
管理员可以在轮播图模块添加轮播图，需要输入轮播图的题目、外链、图片。
### 用户管理
用户管理模块是对所有注册了的用户进行管理。
在用户管理页面，可以查看所有未删除的用户的列表。
管理员可以在用户列表，通过输入用户的账号或者昵称进行搜索，查询后在列表中显示查询后的结果。
管理员可以对用户进行删除、冻结/解冻。
### 教程管理
教程管理模块是对系统中管理员添加的教程进行管理。
进入教程模块，可以查看所有未删除的教程列表。
管理员可以对教程进行删除、禁用/启用和修改。其中修改可以修改教程的标题、介绍、内容、分类、封面图。
删除后的教程不再显示在列表中。
在客户端禁用后的数据用户无法查看。
管理员可以对教程进行新增，新增需要输入教程的标题、介绍、内容、分类、封面图。
管理员可以对教程进行查找，输入教程标题可以搜索出未删除的教程，并且把结果显示在列表中。
### 灵感管理
灵感管理模块是对用户发送的灵感进行管理。
进入灵感模块，可以查看所有未删除的灵感列表。
管理员可以对用户发表的灵感进行删除、禁用/启用。
删除后的教程不再显示到列表中。
在客户端，禁用后的数据仅发表的人自己可见。
管理员可以对灵感进行查找，输入灵感的关键字、或者用户账号、昵称就能够查出对应的结果，并且显示在列表中。
### 文章管理
文章管理模块是对用户发表的文章进行管理。
进入文章管理模块，可以查看所有未删除的文章。
管理员可以对文章进行删除、禁用/启用。
删除后的文章不显示在列表中。
在客户端，禁用后的数据仅发表的人自己可见。
管理员可以对文章进行查找，输入文章的题目关键字、或者用户账号、昵称就能够查出对应的结果，并且显示在列表中。
### 问题管理模块
问题管理模块是对用户发表的问题进行管理。
进入问题管理模块，可以查看所有未删除的问题。
管理员可以对问题进行删除、禁用/启用。
删除后的问题不显示在列表中。
在客户端，禁用后的数据仅发表的人自己可见。
管理员可以对问题进行查找，输入问题的题目关键字、或者用户账号、昵称就能够查出对应的结果，并且显示在列表中。
### 标签管理
标签管理模块是对教程、问题、文章、灵感的分类就行管理。
管理员进入标签管理模块后，可以对不同的类型的标签进行管理。
管理员可以新增标签，需要输入标签类型，标签内容。
标签要求不能大于6个字，不能少于4个字。
管理员可以对已经存在的标签进行修改或者删除。



## 第四章 UI展示
见产品原型图：https://free.modao.cc/app/51252fec556614b352f3fe04a0e61aedc7a28e7b#screen=sk49dm26zh93boq
见产品设计图：附件。


## 第五章 系统角色介绍
测谈网具备四个角色，分别是游客、普通用户、付费用户和管理员。
### 游客
没有登录的用户全部都是游客、游客只具备浏览的权限，并不能有其他的操作。
如果游客想要查看学习教程、面试经验、实战项目以及答题练习模块，则跳转到登录页面。
### 普通用户
所有已经登录的用户就是普通用户，普通用户具备所有非付费模块的操作权限。
包括了用户提问、用户发表文章、用户发表个人灵感、用户学习教程、用户答题练习、实战项目、等功能。
并且用户可以对以上所有内容进行评论、收藏、点赞、关注。
普通用户可以删除自己发表的内容。
### 付费用户
付费用户为浪晋的测试小讲堂付费学生。
在普通用户的权限基础上，具备进入付费模块的权限。
目前付费模块需求未设计。
### 管理员
管理员仅具备测谈网的后台操作权限，可以对用户的所有内容进行管理，包括了用户发表的文章、问题、灵感等内容。
管理员可以对用户进行管理，可以冻结、删除用户。
管理员还具备在后台设置首页信息展示、添加管理教程等功能。


## 第六章 未开发功能说明
目前热门内容、实战项目、答题练习、消息通知、学习小组、面试分享、人才招聘、关于我们、企业合作、友链、意见反馈等内容还未设计和开发。



